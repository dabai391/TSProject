# Git 属性配置 - 强制规范行尾符号 (Line Endings)
# 更多信息请参考: https://git-scm.com/docs/gitattributes

# 默认情况下，让 Git 自动检测文本文件并进行规范化
* text=auto

# 强制所有文本文件在检入仓库时使用 LF (Unix 格式)
# 这可以确保无论是在 Windows 还是 macOS/Linux 上，仓库内的行尾始终保持一致
*.js text eol=lf
*.ts text eol=lf
*.vue text eol=lf
*.css text eol=lf
*.json text eol=lf
*.html text eol=lf
*.md text eol=lf
*.svg text eol=lf
*.cjs text eol=lf
*.mjs text eol=lf

# 显式声明二进制文件，不进行行尾转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
